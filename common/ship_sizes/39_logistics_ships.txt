# Logistics Ship
nomads_constructor = {
	# Logistic Ship handling
	scripted_action = {
		logistic_harvest_resources
		collect_waystation_stockpile
		logistic_drop_off_stockpile
		logistic_cloaking_activate # The action handles whether the button is actually shown
		logistic_cloaking_deactivate # The action handles whether the button is actually shown
	}

	# Keep the vanilla constructor definition in its own inline script for easier patching later
	inline_script = logistic_ships/vanilla_constructor

	required_component_set = "logistic_cloaking_components" # Science Vessels seem to do this and not gate behind DLC
}

nomads_engineer_vessel = {
	# Logistic Ship handling
	scripted_action = {
		logistic_harvest_resources
		logistic_cloaking_activate # The action handles whether the button is actually shown
		logistic_cloaking_deactivate # The action handles whether the button is actually shown
	}

	# Keep the vanilla engineering vessel definition in its own inline script for easier patching later
	inline_script = logistic_ships/vanilla_engineer

	required_component_set = "logistic_cloaking_components"
}
